name: closure
channels:
  - conda-forge
  - nvidia
dependencies:
  - python=3.9
  - cudatoolkit=11.5
  - numpy
  - scipy
  - scikit-learn
  - matplotlib-base
  - jupyterlab
  - ipywidgets
  - tqdm
  - pyqg
  - git
  - pip
  - pip:
    - '--find-links=https://storage.googleapis.com/jax-releases/jax_releases.html'
    - 'jax[cuda]'
    - 'flax'
    - 'optax'
