name: closure
channels:
  - conda-forge
  - nvidia
dependencies:
  - python=3.10
  - cudatoolkit=11.5
  - numpy
  - scipy
  - matplotlib-base
  - jupyterlab
  - tqdm
  - git
  - pip
  - pip:
    - '--find-links=https://storage.googleapis.com/jax-releases/jax_releases.html'
    - 'jax[cuda]'
    - 'flax'
    - 'optax'
